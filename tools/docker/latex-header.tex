\usepackage{sectsty}
\sectionfont{\LARGE\clearpage}

\usepackage{hyperref}

\hypersetup{
  pdftitle={OWASP IoT Security Verification Standard},
  pdfauthor={Aaron Guzman, Cédric Bassem},
  pdfsubject={The IoT Security Verification Standard (ISVS) is a standard for IoT security.},
  pdfkeywords={IoT Security,Internet of Things,OWASP}
}

\usepackage{incgraph,tikz}

% Make "clones" of the commands
\let\originalparagraph\paragraph
\let\originalsubparagraph\subparagraph

% Redefine the commands using the "clones"
\renewcommand{\paragraph}[1]%
{\originalparagraph{#1}\hfill}
\renewcommand{\subparagraph}[1]%
{\originalsubparagraph{#1}\hfill}

% Remove all captions
\renewcommand{\caption}[2][]{}

% TODO for colored tables: add this to pandocker command -V table-use-row-colors=true

\usepackage{geometry}

\geometry{
    a4paper,
    left=20mm,
    top=20mm,
    headheight=40pt,
    voffset=20pt,
    footskip=50pt %40pt gives some more space at the bottom: test it!
}

%%{{CJK}}\usepackage[space]{xeCJK}
%%{{CJK}}\setCJKmainfont{Noto Sans CJK {{CJK-LANG}}} %JP,SC,TC,KR
%%\renewcommand\CJKglue{}% get proper linebreaking if spaces are provided

%%%%\usepackage{xepersian}
%%%%\setlatintextfont{Source Sans Pro}
%%%%\settextfont{FreeFarsi}